(function (root, factory) {
  let Governance = root && root.TSUGUCoreGovernance;
  let HashCore = root && root.TSUGUCoreChangeSet;
  if (typeof module === 'object' && module.exports) {
    try { if (!Governance) Governance = require('./core-governance.js'); } catch {}
    try { if (!HashCore) HashCore = require('./core-changeset.js'); } catch {}
  }
  const api = factory(root, Governance, HashCore);
  if (typeof module === 'object' && module.exports) module.exports = api;
  if (root && root.document) root.TSUGUCoreGovernanceChangeSet = api;
})(typeof globalThis !== 'undefined' ? globalThis : this, function (root, Governance, HashCore) {
  'use strict';
  const SCHEMA_VERSION=1,AGGREGATE_SCHEMA_VERSION=1;
  const OP_TYPES=new Set(['CREATE_TASK','UPDATE_TASK_PLAN','CREATE_APPROVAL','REVOKE_APPROVAL','CREATE_WAIVER','REVOKE_WAIVER','CREATE_DECISION','CREATE_ISSUE','RESOLVE_ISSUE','TASK_HOLD','TASK_INTERRUPT','RESOLVE_TASK_HOLD_REASON','TASK_RESUME_REQUEST','TASK_CANCEL','TASK_START','TASK_RESUME','TASK_COMPLETE']);
  function domainError(code,message,detail){const e=new Error(`${code}: ${message}`);e.code=code;if(detail!==undefined)e.detail=detail;return e;}
  function requireCore(){if(!Governance||typeof Governance.applyOperation!=='function')throw domainError('GOVERNANCE_CORE_REQUIRED','TSUGUCoreGovernance が必要です');return Governance;}
  function requireHash(){if(!HashCore||typeof HashCore.sha256!=='function'||typeof HashCore.stableStringify!=='function')throw domainError('CHANGESET_CORE_REQUIRED','TSUGUCoreChangeSet が必要です');return HashCore;}
  function clone(v){return v==null?v:JSON.parse(JSON.stringify(v));}function deepFreeze(v){if(!v||typeof v!=='object'||Object.isFrozen(v))return v;Object.freeze(v);for(const c of Object.values(v))deepFreeze(c);return v;}function assertObject(v,c,l){if(!v||typeof v!=='object'||Array.isArray(v))throw domainError(c,`${l} が必要です`);return v;}function assertId(v,f){const s=String(v||'').trim();if(!s||s.length>240||/[\u0000-\u001f\u007f]/.test(s))throw domainError('INVALID_ID',`${f} が不正です`);return s;}function assertRevision(v,f='revision'){const n=Number(v);if(!Number.isSafeInteger(n)||n<1)throw domainError('INVALID_REVISION',`${f} は1以上の整数である必要があります`);return n;}function assertBlobSha(v,f='baseBlobSha'){const s=String(v||'').trim().toLowerCase();if(!/^[a-f0-9]{40}$/.test(s))throw domainError('INVALID_BLOB_SHA',`${f} は40桁Git blob SHAである必要があります`);return s;}function nowIso(v){const d=v==null?new Date():new Date(v);if(Number.isNaN(d.getTime()))throw domainError('INVALID_TIMESTAMP','時刻が不正です');return d.toISOString();}function stableStringify(v){return requireHash().stableStringify(v);}function sha256(v){return requireHash().sha256(v);}
  function normalizeOperation(input){const op=clone(assertObject(input,'GOVERNANCE_OPERATION_REQUIRED','operation'));const type=String(op.type||'').toUpperCase();if(!OP_TYPES.has(type))throw domainError('UNSUPPORTED_GOVERNANCE_OPERATION',type||'<empty>');for(const f of ['approvedBy','approver','approverId','actor','actorId','actorKind','role','waivedBy','decidedBy','resolvedBy'])if(Object.prototype.hasOwnProperty.call(op,f))throw domainError('ACTOR_AUTHORITY_FROM_INPUT_REJECTED',`${f} はoperationへ指定できません`);if(op.at==null)throw domainError('INVALID_TIMESTAMP',`${type}.at が必要です`);nowIso(op.at);delete op.type;return deepFreeze({type,...op});}
  function payloadProjection(c){return{type:'GovernanceChangeSet',schemaVersion:SCHEMA_VERSION,id:c.id,projectId:c.projectId,idempotencyKey:c.idempotencyKey,baseRevision:c.baseRevision,baseBlobSha:c.baseBlobSha,operations:c.operations};}
  function createChangeSet(input){const r=assertObject(input,'GOVERNANCE_CHANGESET_REQUIRED','GovernanceChangeSet');const out={type:'GovernanceChangeSet',schemaVersion:SCHEMA_VERSION,id:assertId(r.id,'GovernanceChangeSet.id'),projectId:assertId(r.projectId,'GovernanceChangeSet.projectId'),idempotencyKey:assertId(r.idempotencyKey,'GovernanceChangeSet.idempotencyKey'),baseRevision:assertRevision(r.baseRevision),baseBlobSha:assertBlobSha(r.baseBlobSha),operations:(r.operations||[]).map(normalizeOperation)};if(!out.operations.length)throw domainError('EMPTY_GOVERNANCE_CHANGESET','operationが必要です');out.payloadHash=sha256(payloadProjection(out));return deepFreeze(out);}
  function validateChangeSetShape(input){const cs=createChangeSet(input);if(input.payloadHash!=null&&input.payloadHash!==cs.payloadHash)throw domainError('GOVERNANCE_CHANGESET_PAYLOAD_HASH_MISMATCH','payloadHashが一致しません');return cs;}
  function createAggregate(registryInput){const registry=requireCore().validateState(registryInput);return validateAggregate({type:'GovernanceAggregate',schemaVersion:AGGREGATE_SCHEMA_VERSION,projectId:registry.projectId,revision:registry.revision,registry,appliedChangeSets:[],auditEvents:[],outbox:[]});}
  function validateAggregate(input){const r=clone(assertObject(input,'GOVERNANCE_AGGREGATE_REQUIRED','GovernanceAggregate'));if(r.type!=='GovernanceAggregate'||Number(r.schemaVersion)!==AGGREGATE_SCHEMA_VERSION)throw domainError('UNSUPPORTED_GOVERNANCE_AGGREGATE_SCHEMA','GovernanceAggregate契約が不正です');const registry=requireCore().validateState(r.registry),projectId=assertId(r.projectId,'projectId'),revision=assertRevision(r.revision);if(registry.projectId!==projectId)throw domainError('PROJECT_SCOPE_VIOLATION','Aggregate Projectが一致しません');if(registry.revision!==revision)throw domainError('GOVERNANCE_AGGREGATE_REVISION_MISMATCH','revisionが一致しません');return deepFreeze({type:'GovernanceAggregate',schemaVersion:AGGREGATE_SCHEMA_VERSION,projectId,revision,registry,appliedChangeSets:Array.isArray(r.appliedChangeSets)?r.appliedChangeSets:[],auditEvents:Array.isArray(r.auditEvents)?r.auditEvents:[],outbox:Array.isArray(r.outbox)?r.outbox:[]});}
  function actorProjection(actor){const a=requireCore().normalizeActor(actor);return{id:a.id,subject:a.subject,kind:a.kind,role:a.role};}
  function targetIds(ops){const s=new Set();for(const op of ops)for(const k of ['id','taskId','approvalId','waiverId','issueId'])if(op[k]!=null)s.add(String(op[k]));return[...s].sort();}
  function validateChangeSet(aggregateInput,currentBlobSha,changeSetInput,options={}){const aggregate=validateAggregate(aggregateInput),blobSha=assertBlobSha(currentBlobSha,'currentBlobSha'),cs=validateChangeSetShape(changeSetInput),actor=actorProjection(options.actor);if(aggregate.projectId!==cs.projectId)throw domainError('PROJECT_SCOPE_VIOLATION','ChangeSet Projectが一致しません');if(aggregate.revision!==cs.baseRevision||blobSha!==cs.baseBlobSha)throw domainError('STALE_CHANGESET','base revision/blob SHAが現在状態と一致しません');let candidate=aggregate.registry;for(const op of cs.operations)candidate=requireCore().applyOperation(candidate,op,{actor,now:op.at});candidate=requireCore().validateState(candidate);const record={type:'GovernanceValidationRecord',schemaVersion:SCHEMA_VERSION,id:options.validationId?assertId(options.validationId,'validationId'):`governance-validation:${cs.id}`,changeSetId:cs.id,projectId:cs.projectId,baseRevision:cs.baseRevision,baseBlobSha:cs.baseBlobSha,payloadHash:cs.payloadHash,candidateHash:sha256(requireCore().serialize(candidate)),actorHash:sha256(actor),actor,targetIds:targetIds(cs.operations),resultRevision:candidate.revision,status:'PASS',issues:[],validatedAt:nowIso(options.now)};return deepFreeze({validationRecord:record,candidate});}
  function validateRecord(record,cs,candidate,actor){const r=assertObject(record,'GOVERNANCE_VALIDATION_RECORD_REQUIRED','GovernanceValidationRecord');if(r.type!=='GovernanceValidationRecord'||r.status!=='PASS')throw domainError('VALIDATION_NOT_PASS','PASSのValidationRecordが必要です');if(r.changeSetId!==cs.id||r.projectId!==cs.projectId||r.baseRevision!==cs.baseRevision||r.baseBlobSha!==cs.baseBlobSha||r.payloadHash!==cs.payloadHash||r.actorHash!==sha256(actor))throw domainError('GOVERNANCE_VALIDATION_RECORD_MISMATCH','ValidationRecord対象が一致しません');if(r.candidateHash!==sha256(requireCore().serialize(candidate))||r.resultRevision!==candidate.revision)throw domainError('GOVERNANCE_VALIDATION_RECORD_MISMATCH','candidateが一致しません');return r;}
  function prepareApply(aggregateInput,currentBlobSha,changeSetInput,validationRecord,options={}){const aggregate=validateAggregate(aggregateInput),blobSha=assertBlobSha(currentBlobSha,'currentBlobSha'),cs=validateChangeSetShape(changeSetInput),actor=actorProjection(options.actor);const existing=aggregate.appliedChangeSets.find(x=>x.idempotencyKey===cs.idempotencyKey);if(existing){if(existing.payloadHash!==cs.payloadHash)throw domainError('IDEMPOTENCY_KEY_REUSED','同一idempotency keyが異なるpayloadで使用済みです');return deepFreeze({status:'IDEMPOTENT_REPLAY',expectedBlobSha:blobSha,aggregate,result:{changeSetId:existing.changeSetId,revision:existing.resultRevision,candidateHash:existing.candidateHash,idempotencyKey:existing.idempotencyKey}});}const rerun=validateChangeSet(aggregate,blobSha,cs,{now:options.now,validationId:validationRecord&&validationRecord.id,actor});validateRecord(validationRecord,cs,rerun.candidate,actor);const at=nowIso(options.now),applied={type:'AppliedGovernanceChangeSet',changeSetId:cs.id,idempotencyKey:cs.idempotencyKey,payloadHash:cs.payloadHash,candidateHash:rerun.validationRecord.candidateHash,baseRevision:cs.baseRevision,resultRevision:rerun.candidate.revision,actorId:actor.id,actorKind:actor.kind,appliedAt:at},audit={type:'AuditEvent',action:'APPLY_GOVERNANCE_CHANGESET',changeSetId:cs.id,actorId:actor.id,actorKind:actor.kind,baseRevision:cs.baseRevision,resultRevision:applied.resultRevision,payloadHash:cs.payloadHash,candidateHash:applied.candidateHash,at},outbox={type:'OutboxRecord',id:`outbox:${cs.id}`,eventType:'GOVERNANCE_CHANGED',changeSetId:cs.id,projectId:cs.projectId,resultRevision:applied.resultRevision,candidateHash:applied.candidateHash,status:'PENDING',createdAt:at};const next=validateAggregate({type:'GovernanceAggregate',schemaVersion:AGGREGATE_SCHEMA_VERSION,projectId:aggregate.projectId,revision:rerun.candidate.revision,registry:rerun.candidate,appliedChangeSets:[...aggregate.appliedChangeSets,applied],auditEvents:[...aggregate.auditEvents,audit],outbox:[...aggregate.outbox,outbox]});return deepFreeze({status:'READY',expectedBlobSha:blobSha,aggregate:next,serialized:stableStringify(next),result:{changeSetId:cs.id,revision:next.revision,candidateHash:applied.candidateHash,idempotencyKey:cs.idempotencyKey}});}
  function b64enc(text){if(typeof Buffer!=='undefined')return Buffer.from(text,'utf8').toString('base64');return btoa(unescape(encodeURIComponent(text)));}function b64dec(text){if(typeof Buffer!=='undefined')return Buffer.from(String(text).replace(/\n/g,''),'base64').toString('utf8');return decodeURIComponent(escape(atob(String(text).replace(/\n/g,''))));}
  async function githubJson(fetchImpl,url,options={}){const response=await fetchImpl(url,options),text=await response.text();let body=null;try{body=text?JSON.parse(text):null;}catch{body={message:text};}return{response,body};}
  async function applyWithContentApi(options){const fetchImpl=options&&options.fetch||(root&&root.fetch);if(typeof fetchImpl!=='function')throw domainError('FETCH_REQUIRED','fetch実装が必要です');const owner=assertId(options.owner,'owner'),repo=assertId(options.repo,'repo'),branch=assertId(options.branch,'branch'),actor=actorProjection(options.actor);const path=String(options.path||'').replace(/^\/+/, '');if(!path)throw domainError('INVALID_PATH','保存pathが必要です');const headers={Accept:'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28',...(options.headers||{})};if(options.token)headers.Authorization=`Bearer ${options.token}`;const api=`https://api.github.com/repos/${encodeURIComponent(owner)}/${encodeURIComponent(repo)}/contents/${path.split('/').map(encodeURIComponent).join('/')}`;const read=await githubJson(fetchImpl,`${api}?ref=${encodeURIComponent(branch)}`,{headers});if(!read.response.ok)throw domainError('GITHUB_READ_FAILED',`GitHub read ${read.response.status}`,read.body);const blobSha=assertBlobSha(read.body.sha,'GitHub content sha');let aggregate;try{aggregate=JSON.parse(b64dec(read.body.content));}catch{throw domainError('INVALID_GOVERNANCE_AGGREGATE_JSON','Governance aggregate JSONを解析できません');}const prepared=prepareApply(aggregate,blobSha,options.changeSet,options.validationRecord,{now:options.now,actor});if(prepared.status==='IDEMPOTENT_REPLAY')return deepFreeze({...prepared.result,status:'IDEMPOTENT_REPLAY',blobSha,commitSha:null});const write=await githubJson(fetchImpl,api,{method:'PUT',headers:{...headers,'Content-Type':'application/json'},body:JSON.stringify({message:options.message||`Apply ${prepared.result.changeSetId}`,content:b64enc(prepared.serialized),sha:prepared.expectedBlobSha,branch})});if(!write.response.ok){if(write.response.status===409||write.response.status===422)throw domainError('STALE_CHANGESET','GitHub CASに失敗しました。部分変更は確定されていません',write.body);throw domainError('GITHUB_WRITE_FAILED',`GitHub write ${write.response.status}`,write.body);}const commitSha=write.body&&write.body.commit&&write.body.commit.sha||null,writtenBlobSha=write.body&&write.body.content&&write.body.content.sha||null,readbackRef=commitSha||branch;const readback=await githubJson(fetchImpl,`${api}?ref=${encodeURIComponent(readbackRef)}`,{headers});if(!readback.response.ok)throw domainError('GITHUB_READBACK_FAILED',`GitHub readback ${readback.response.status}`,readback.body);if(writtenBlobSha&&readback.body&&readback.body.sha!==writtenBlobSha)throw domainError('APPLY_READBACK_MISMATCH','Governance Apply readback blob SHAが書込み結果と一致しません');const stored=validateAggregate(JSON.parse(b64dec(readback.body.content))),cs=validateChangeSetShape(options.changeSet),applied=stored.appliedChangeSets.find(x=>x.changeSetId===cs.id&&x.payloadHash===cs.payloadHash);if(!applied||stored.revision!==prepared.result.revision)throw domainError('APPLY_READBACK_MISMATCH','Governance Apply readbackが一致しません');return deepFreeze({...prepared.result,status:'APPLIED',blobSha:readback.body.sha,commitSha});}
  return deepFreeze({SCHEMA_VERSION,AGGREGATE_SCHEMA_VERSION,OP_TYPES:Object.freeze([...OP_TYPES]),stableStringify,sha256,createChangeSet,validateChangeSetShape,createAggregate,validateAggregate,validateChangeSet,prepareApply,applyWithContentApi});
});
